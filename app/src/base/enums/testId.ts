const enum TestId {
  formCard = 'form-card',
  mainCard = 'main-card',
  dateInput = 'date-input',
  imageInput = 'image-input',
  popup = 'popup',
  closeButton = 'close-button',
  loading = 'loading',
}

export default TestId;
